server:
  port: 8082
management:
  tracing:
    sampling:
      probability: 1.0
logging:
  pattern:
    level: "%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]"
spring:
  application:
    name: notification
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/notification
    username: amigoscode
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  zipkin:
    base-url: http://localhost:9411
  rabbitmq:
    addresses: localhost:5672
  mail:
    host: smtp.gmail.com
    port: 587
    username: yassermoussaoui004@gmail.com
    password: hmvg wsbs mkkf bkcj  # Not my real password haha
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
rabbitmq:
  exchanges:
    internal: internal.exchange
  queues:
    notification : notification.queue
  routing-keys:
    internal-notification: internal.notification.routing-key